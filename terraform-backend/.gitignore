# .gitignore
**/.terraform/** 
.terraform/**/** 


# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version control as
# they often contain secrets. If you have other secrets in plaintext, you may want to
# exclude those files as well.
*.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfplan files in .gitignore as they can be used to see the full plan to apply
# and might contain sensitive data as well.
*.tfplan

# Include CLI configuration files
.terraformrc
terraform.rc
